// Pièces gagnees par rarete de spot
export const COINS_PER_RARITY: Record<string, number> = {
  commun:    1,
  rare:      2,
  epique:    3,
  legendaire:4,
  platine:   5,
};

// Pièces bonus a la completion d'une quête
export const COINS_PER_QUEST = 2;
